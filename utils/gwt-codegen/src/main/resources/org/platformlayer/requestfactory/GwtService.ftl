package ${gwtPackage}.server;

import ${modelPackage}.${className};
import org.platformlayer.ui.shared.server.GwtServiceBase;

public class ${serviceClassName} extends GwtServiceBase<${className}> {
    public ${serviceClassName}() {
        super(${className}.class);
    }
}