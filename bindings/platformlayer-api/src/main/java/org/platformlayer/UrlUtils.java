package org.platformlayer;

public class UrlUtils {

	// public static String toUrl(ModelKey modelKey) {
	// String url = modelKey.getProject().toString() + "/" + modelKey.getServiceType().toString() + "/" +
	// modelKey.getItemType().toString();
	// if (modelKey.getItemKey() != null) {
	// url += "/" + modelKey.getItemKey().getKey();
	// }
	// return url;
	// }

}
