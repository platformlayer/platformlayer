package org.platformlayer.ops.pool;

public abstract class PoolBuilderBase<T> implements PoolBuilder<T> {
	// private static final Logger log = LoggerFactory.getLogger(PoolResourceAdapterBase.class);

	// protected String toKey(T item) {
	// return item.toString();
	// }

	// protected abstract Properties buildProperties(T item);
}
